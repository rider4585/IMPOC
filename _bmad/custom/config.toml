# Team / enterprise overrides for _bmad/config.toml.
# Committed to the repo — applies to every developer on the project.
# Tables deep-merge over base config; keyed entries merge by key.
# Example: override an agent descriptor, or add a new agent.
#
# [agents.bmad-agent-pm]
# description = "Prefers short, bulleted PRDs over narrative drafts."

# Custom Skill: Postman Collection Generator
[skills.bmad-postman-collection]
module = "bmm"
display_name = "Postman Collection Generator"
menu_code = "PC"
description = "Scans API codebase and generates comprehensive Postman collection with role-based tests, auth flows, and request examples."
phase = "ship"
preceded_by = "bmad-build"
action = "generate"
output_location = "{project-root}/postman"
outputs = "postman-collection.json"
