---
topic: IMPOC — inventory, POS, and rental management for SHREE Fashion Store, driven by a unique Code 128 barcode per physical unit
updated: 2026-08-29T08:51
---

- (event) create run from in-chat brain dump; slug impoc-core chosen (whole unbuilt core of IMPOC)
- (note) read AGENTS.md, backend/AGENTS.md, frontend/AGENTS.md as durable conventions; adopted as companions rather than restated
- (note) read backend/src/constants/permissions.js, barcode module (constants/service/controller/routes), role + role-permission seeders, users model+migration
- (direction) user: already built = JWT access+refresh auth w/ argon2, RBAC 5 roles, permission constants (inventory/sales/expenses/reports), bwip-js+pdfkit barcode PDF module, Code 128 camera scanner in React SPA; everything else unbuilt
- (decision) CAP-1 barcode sheets: A4 grid PDF from a page count, barcode 35mm x 8mm, number text 5pt, 15pt clear writing space under each label
- (decision) input said 'open endpoint' but also 'restrict to ADMIN and INVENTORY_MANAGER'; backend/AGENTS.md flags the current unauthenticated mount as a policy violation, so restricted wins — 'open' read as 'exposed', not 'unauthenticated'
- (decision) barcode print guard is a new permission constant (inventory.barcode_generate) seeded to ADMIN + INVENTORY_MANAGER only, not a role literal — MANAGER already holds inventory.create so an inventory.create guard would leak access, and backend/AGENTS.md forbids string-literal permissions
- (constraint) defect to reconcile: PDF_CONFIG in backend/src/modules/barcode/barcode.constants.js says A2; spec requires A4
- (constraint) defect to reconcile: barcode values come from Date.now()+index and are never persisted — cannot satisfy unique/non-repeating across requests; issued values must be persisted (CAP-2)
- (constraint) defect to reconcile: barcode.controller.js builds 400 JSON inline, contradicting backend/AGENTS.md (Zod validation + error middleware only)
- (constraint) defect to reconcile: frontend BarcodeScanner.jsx imports @zxing/library which is not declared in package.json
- (decision) intake model formalised as given: ProductType (self-referencing parentId), Vendor, StockIntake (trip: vendor/date/billRef/totalPaid), StockIntakeLine (the lot: type, name, qty, buying/selling/floor price, rental terms), Unit (barcode, colour, size, status, channel, price snapshots)
- (decision) no reusable product-template table — prices change every trip, a saved template goes stale
- (decision) colour and size are picklists, never free text; channel lives on Unit defaulted from the lot so retail/rental conversion needs no migration
- (decision) intake UX: open a lot, scan, enter only colour+size, repeat, with an 'N of M scanned' progress counter; plus clone-last-lot (CAP-8) and size-run auto-advance (CAP-9)
- (decision) unit states: retail in_stock<->sold, side exits damaged/lost; rental in_stock->reserved(date range)->rented->in_stock or in_maintenance->in_stock or retired, side exits lost/retired
- (decision) in_cart is NOT a unit state — carts hold references; checkout re-verifies status==in_stock inside the transaction, and a unique constraint on unit_id in sale lines / rental agreements is the race guard
- (decision) retail POS: scan to cart; collect customer name, WhatsApp number, DOB, email before checkout; returning customer found by WhatsApp number alone; payment method UPI or cash recorded only, no gateway; sale below unit floor price is refused
- (decision) rental: one agreement per unit, agreements share a group id for one receipt so partial returns need no special handling; terms are rentPerDay, deposit (paid in full up front), overduePerDay > rentPerDay, deducted from deposit; max period = floor(deposit/overduePerDay) derived; overdue = rented AND today > dueDate derived; return by scanning the same barcode
- (decision) expenses cover shop rent, maintenance, non-sellable purchases, and rental-unit upkeep
- (decision) dashboard is the headline feature; the question catalog lives in dashboard-questions.md, 19 questions across takings, inventory, rental, vendor/lot, discounting, staff, customers
- (constraint) money as integer paise; two-level snapshot (unit copies lot prices at intake, sale/rental line copies transacted price at checkout, customer name+number snapshotted onto the transaction); never UPDATE a completed transaction, record a reversing row; timestamps + soft delete on every table; online only, no offline/sync layer
- (decision) companions: domain-model.md, unit-state-machine.md, dashboard-questions.md, brownfield.md, glossary.md (spec-authored) + the three AGENTS.md files (adopted)
- (assumption) '5px' text and '15px' clear space read as PDF points (pdfkit's native unit); barcode 35mm x 8mm stays millimetres
- (assumption) printed barcode values must be persisted at issue time (a barcode_issue row) so uniqueness survives restarts and concurrent requests; intake binds an issued, unbound value to a unit
- (assumption) reserved -> in_stock is a legal transition (reservation cancelled or lapsed); the input listed no cancel path but reservations are otherwise unrecoverable
- (assumption) in_stock -> rented directly (walk-in, no prior reservation) is legal; reserved is an optional pre-step
- (assumption) a retail return refunds the transacted price in full via a reversing row; partial refunds and exchanges are not specified
- (assumption) declaring an overdue rental unit lost is a manual staff action — with no cron, nothing can auto-transition it
- (assumption) single shop, single physical location, single currency (INR)
- (question) Do rental operations reuse the SALES.* permission group or get a new RENTALS.* group in permissions.js? The existing constants cover inventory/sales/expenses/reports only.
- (question) Is a GST-compliant tax invoice required for sales, and does rental income attract GST? The input is silent and this is an Indian retail shop.
- (question) Can damage or cleaning charges be deducted from a rental deposit, or is overdue the only deduction?
- (question) Must intake reject a scanned barcode the system never issued, or accept any unique value?
- (question) Is the rental due date fixed at reservation time or at hand-over?
- (question) Is any identity document recorded at rental hand-over? Nothing in the input covers rental-risk identification beyond the deposit.
- (question) Customer DOB, WhatsApp number, and email are collected — is there a retention or consent requirement (DPDP Act) the build must honour? DOB currently has no consumer; it exists for future occasion-based offers.
- (question) Retail return: is there a time window, and does the unit re-enter stock at its original selling price snapshot or the lot's current one?
- (note) dashboard catalog landed at 21 questions (Q1-Q21), not 19 as logged earlier
- (event) self-validate pass 1 (coherence): PASS — 22 capabilities CAP-1..CAP-22, every one carrying both intent and success; ids unique and sequential; 20 constraints all rule something out; 9 non-goals; success signal is a concrete end-to-end demonstration; 5 spec-authored + 3 adopted companions all resolve on disk
- (event) self-validate pass 2 (preservation): PASS — every load-bearing source claim traced to SPEC.md or a companion; two errors caught and fixed: the example trip is 48 pieces not 46, and it is two vendor trips on one day (StockIntake is one vendor per trip), not one trip across two vendors; ProductType examples restored in full (U-neck, Tissue Silk). No wrapper-only content dropped.
- (event) spec finalized: _bmad-output/specs/spec-impoc-core/ — 8 assumptions, 8 open questions outstanding
- (decision) OQ resolved (Raviraj): rental operations get their own RENTALS.* permission group in permissions.js, seeded per role — not a reuse of SALES.*, so cashier selling rights and rental hand-over rights can be granted separately
- (decision) OQ resolved (Raviraj): no GST in scope — the shop is not GST-registered. No tax columns, no HSN, no invoice series; the receipt stays a plain receipt and dashboard takings are the full transacted amount. Registration is expected later, so GST becomes a non-goal with a do-not-block-this-later note (same shape as loyalty and vendor returns)
- (decision) OQ resolved (Raviraj): damage and cleaning charges ARE deductible from the rental deposit at settlement, alongside overdue. Staff pick a graded condition from a new admin-maintained picklist (DamageGrade) running from light cleaning through repairable damage to beyond-repair/retire; each grade carries a default charge in paise that pre-fills the deduction and staff may override per case. The grade and the charged amount are snapshotted onto the agreement at settlement. Deposit balance still floors at zero
- (decision) Raviraj: money collected as a damage/cleaning charge is earmarked to repair or replace the unit — recorded as income against that unit, with the actual repair booked as a RENTAL_UPKEEP expense against the same unit so both sides show on the unit's ledger
- (decision) Raviraj: a beyond-repair grade at settlement retires the unit directly (new transition rented -> retired at return, replacing the mandatory in_maintenance hop for that case), and retirement triggers a final ROI figure for the unit: lifetime rental income + damage charges collected, minus upkeep, against buying price
- (decision) OQ resolved (Raviraj) — OVERTURNS the persisted-issue-register design: the system does NOT record which barcode values it has printed. Sheets are generated in advance and the system never knows which codes exist on paper. Intake therefore accepts ANY scanned value that is not already bound to a unit; there is no issued-and-unbound check
- (decision) Raviraj: uniqueness moves from a register to the generator — one persistent counter row (high-water mark) hands out strictly increasing zero-padded values, replacing the Date.now()+index scheme. Never repeats, survives restart and database restore, stays short enough to scan in a 35mm x 8mm Code 128 label. The counter is the only barcode state stored; BarcodeBatch and BarcodeIssue entities are dropped from the domain model
- (decision) Consequence: Unit.barcode's unique constraint becomes the sole binding guard (a value already bound to a unit is refused, naming that unit). Brownfield D2 is restated — the defect is the collision-prone generator, not the absence of persistence
- (decision) OQ resolved (Raviraj): the rental due date is fixed at BOOKING, not at hand-over. A booking names start date x and end date y; rent for the whole window PLUS the security deposit are collected in full at booking time, not at hand-over. Hand-over is the physical release of the piece only — no money changes hands then. Collecting a day late does not extend the window or reduce the rent; the customer pays what they agreed. Returning after y accrues overdue, deducted from the deposit
- (decision) Raviraj: rental availability is BY DATE RANGE, not by a blocking status. A unit booked for the 14th-17th is still rentable for a non-overlapping 5th-7th window. 'reserved' ceases to be a unit status; availability becomes a query for overlapping open bookings, and the unit's status is in_stock until it physically leaves (rented) — a material rewrite of unit-state-machine.md
- (decision) Raviraj: prepaid rent is never pro-rated. Early return refunds no rent; a cancellation or no-show forfeits the rent and returns the deposit in full. Settlement therefore computes only overdue and damage/cleaning against the deposit — no day-counting of rent at return
- (direction) Raviraj deferred the rental identity-document question — it stays an open question in SPEC.md; hand-over captures nothing beyond the customer record until it is answered
- (decision) OQ resolved (Raviraj): DPDP obligations ARE in scope. Customer capture records explicit consent — a flag with timestamp and stated purpose on the Customer record — and an admin flow can erase or anonymise a customer's personal fields (name, WhatsApp, DOB, email) on request while leaving sale and rental history intact. The existing name/WhatsApp snapshots on Sale and RentalAgreement must therefore be anonymisable too, or the erase is cosmetic. New capability + Customer schema change
- (decision) Raviraj: erase-on-request reaches the Customer record only. Name, WhatsApp, DOB and email on the Customer row are anonymised and the WhatsApp lookup key is released; the name/number snapshots on past sales and rental agreements are left intact, preserving the never-UPDATE constraint absolutely and keeping receipts reproducible. Erase is a supersede-safe operation on Customer, not a history rewrite
- (decision) OQ resolved (Raviraj) — REPLACES CAP-16: the shop has NO return policy, only an exchange policy. There is no refund-a-sale flow at all. A customer brings back a sold unit and swaps it for another unit: the returned unit's sale line is reversed and the unit goes back to in_stock; the outgoing unit is sold on a new line in the same transaction
- (decision) Raviraj: exchange price difference — if the new piece is dearer the customer pays the difference (UPI or cash, recorded as takings); if it is cheaper the shop refunds the difference in cash. Both directions net through the dashboard's takings figures
- (decision) Raviraj: exchange window is 7 days from the sale date, admin-configurable rather than hard-coded, enforced server-side and refused past the window naming the sale date. The returned unit re-enters stock carrying its ORIGINAL buying/selling/floor snapshots, not the lot's current values
- (decision) ASSUMPTION CONFIRMED (Raviraj), with an addition: '5px' and '15px' are PDF points (5pt number text, 15pt clear writing space); the barcode stays 35mm x 8mm. Additionally ALL label dimensions — barcode width and height, text size, clear space, margins, and the resulting grid — are configurable values rather than hard-coded literals, so the layout can be tuned against a physical print without a code change
- (decision) ASSUMPTION CONFIRMED (Raviraj): walk-in rentals are legal — booking and hand-over collapse into one transaction (pick return date, collect rent + deposit, unit goes straight to rented). A prior booking is an optional pre-step. The overlapping-booking availability check still applies to the requested window
- (note) Assumptions 2 (issued values persisted), 3 (reserved -> in_stock legal) and 5 (retail return refunds in full) were resolved implicitly by Raviraj's answers to open questions 4, 5 and 8 — not re-asked
- (decision) ASSUMPTION CONFIRMED (Raviraj): declaring an overdue rental unit lost is a manual staff action; nothing auto-transitions it. Deposit is forfeited as income at that moment and the unit becomes terminal. The no-scheduler constraint holds
- (decision) ASSUMPTION CONFIRMED (Raviraj): single shop, single physical location, single currency (INR). No store/location dimension and no currency column on any table; multi-store stays a non-goal
- (decision) ASSUMPTION CONFIRMED (Raviraj): barcode printing is guarded by a new permission constant (inventory.barcode_generate) seeded to ADMIN and INVENTORY_MANAGER only — not inventory.create, which MANAGER already holds
- (decision) ASSUMPTION CONFIRMED (Raviraj): 'open endpoint' means exposed to the SPA, not unauthenticated. /generate and /test-sheet get auth middleware plus the permission guard; brownfield D3 is a real defect the build fixes
- (event) assumption/open-question walkthrough complete: 8 open questions answered (1 deferred: rental identity document), 9 assumptions resolved (6 confirmed directly, 3 superseded by open-question answers). Re-derive follows
- (event) self-validate pass 1 (coherence): PASS — 23 live capabilities, each with intent and success; CAP-16 retired (never reused) and recorded as such; 21 constraints all rule something out; 13 non-goals; success signal rewritten as a concrete end-to-end demonstration covering exchange, booking-time payment and damage grading; 5 spec-authored + 3 adopted companions all resolve on disk
- (event) self-validate pass 2 (preservation): PASS — all 17 walkthrough answers traced into SPEC.md or a companion; no stale references to reserved-as-status, BarcodeIssue/BarcodeBatch, or the retired retail-return flow survive except as explicit negative statements. One new assumption recorded (damage charge is income against the unit, the repair it funds is a RENTAL_UPKEEP expense) and one open question retained by Raviraj's deferral (rental identity document)
- (event) spec re-derived after the walkthrough: 1 assumption, 1 open question outstanding (down from 9 and 8). No stories.yaml exists, so nothing to re-check there. sources: [] — the input was an in-chat brain dump, so there is no upstream file to sync back
- (decision) CAP-22 reconciliation criterion CORRECTED (Raviraj, from architecture AD-13 / v_deposits_held): deposits held is summed from rental_bookings.deposit_paise over state IN ('open','handed_over'), minus deposit already returned at settlement. The prior wording 'the sum of open agreements' deposits' understates the liability by every booking that is paid but not yet handed over — deposits are collected at BOOKING, and such a booking holds real cash with no agreement row in existence
- (constraint) RentalAgreement.depositPaise is a settlement-arithmetic snapshot and is never summed as a liability figure; the deposit liability has exactly one source, RentalBooking. Recorded in domain-model.md and dashboard-questions.md Q6 so the agreement-summing error cannot be re-derived
- (question) AD-13's v_deposits_held contract reads 'from open and handed_over bookings AND from open agreements' — under the corrected rule that double-counts every handed_over booking, which always has an open agreement. Architecture-owned (bmad-architecture), not fixed here; flagged to Raviraj
- (event) self-validate pass 1 (coherence): PASS — CAP-22 keeps intent + success; the reconciliation identity is now falsifiable against rows and stays at domain level in the kernel (booking state, not table names), with the column-level sourcing in dashboard-questions.md Q6 where it belongs. No other capability, constraint or companion asserts a deposit figure
- (event) self-validate pass 2 (preservation): PASS — swept all 5 spec-authored companions for the agreement-summing error. glossary.md ('collected in full at booking') and unit-state-machine.md were already correct; domain-model.md and dashboard-questions.md Q6 both patched, plus a new binding dashboard rule naming RentalBooking as the sole liability source. Remaining 'open agreements' hits (domain-model.md:162 race guard, dashboard-questions.md Q11 out-on-rent, SPEC.md:125) are unrelated and correct
- (event) targeted re-derive complete: CAP-22 success criterion + domain-model.md (agreement snapshot row, money rule 3) + dashboard-questions.md (Q6 row, new binding rule). Capability set unchanged at 23 live; no stories.yaml exists; sources: [] so nothing upstream to sync
- (decision) Deposit liability CORRECTED again (Raviraj, from architecture AD-9 + AD-13): the 'minus RentalAgreement.depositReturnedPaise' clause is DROPPED from dashboard-questions.md Q6, SPEC.md CAP-22 and domain-model.md money rule 3. AD-9 now requires settlement to advance the booking HANDED_OVER -> SETTLED in the same transaction that writes the agreement's returned_at, so a settled booking has already left the open/handed_over predicate — subtracting the returned deposit again drives Q6 negative by the whole history of returned deposits. The liability is the state predicate alone
- (constraint) Nothing is ever subtracted from the deposit-liability sum. A deposit leaves Q6 in exactly one way: the booking's state leaves ('open','handed_over') — settled at settlement, cancelled at cancellation, written off when the piece never returns. depositReturnedPaise is a cash-movement figure whose only reader is Q2's till reconciliation
- (note) Q11 (out on rent right now) is a live-state question reading open RentalAgreement rows directly, not a v_deposits_held reader — dashboard-questions.md Q11 already says exactly that, so no spec change was needed. The mis-listing was architecture-side and is already corrected in AD-13 of architecture-IMPOC-2026-08-20/ARCHITECTURE-SPINE.md. The prior memlog question about v_deposits_held double-counting handed_over bookings is now answered there too: the view is sourced from rental_bookings alone
- (constraint) Settling a rental records the agreement's return and moves the booking handed_over -> settled as one indivisible act (architecture AD-9). Recorded in unit-state-machine.md because Q6's correctness now rests on it: a booking left at handed_over after the piece is back holds its deposit in the liability forever and keeps its dates blocking the floor
- (event) self-validate pass 1 (coherence): PASS — CAP-22 keeps intent + success and its reconciliation identity is still falsifiable against rows, now as a pure state predicate. Capability set unchanged (23 live, CAP-16 still retired); 21 constraints, 13 non-goals, success signal untouched; all 5 spec-authored and 3 adopted companions resolve on disk
- (event) self-validate pass 2 (preservation): PASS — swept every companion for a deposit-deduction claim. Three carried it and all three were patched (dashboard-questions.md Q6, SPEC.md CAP-22, domain-model.md money rule 3); glossary.md was already silent on it. Surviving deduct/minus hits are unrelated and correct: CAP-19 and constraint 'settlement deducts only overdue and damage from the deposit' is per-agreement arithmetic on one row, not the aggregate liability; Q1's 'minus reversing lines'; glossary Final ROI. One gap closed beyond the request — unit-state-machine.md never stated that settlement moves the booking out of handed_over, which is the fact Q6's correctness now rests on
- (event) targeted re-derive complete: SPEC.md CAP-22 + dashboard-questions.md (Q6 row, new binding rule) + domain-model.md money rule 3 + unit-state-machine.md (settlement atomicity line). No stories.yaml exists. sources: [] so nothing upstream to sync; the one open upstream item is the architecture's own AD-13 'Conflict with the spec' note, now satisfied and clearable by bmad-architecture
- (event) update run: input is the Upstream Conflicts table U-1..U-12 in architecture-IMPOC-2026-08-20/ARCHITECTURE-SPINE.md (Raviraj's instruction). Spine is architecture-owned and is NOT edited by this run; every AD id referenced stays as the spine numbers it
- (decision) U-1 RESOLVED (architecture AD-33): rental income is NO LONGER anchored to the day the booking was made. Rent and deposit are still both collected in full at booking (CAP-17 unchanged) but are a LIABILITY until the booking reaches a close event; income is recognised at the close event and anchored to that event's shop day. Supersedes the earlier 'rental income lands in the range the booking was made / a settlement moves no revenue' rule in SPEC.md CAP-17+CAP-22, dashboard-questions.md Q1 + binding rules, and domain-model.md RentalBooking + money rule 3
- (decision) U-2 RESOLVED (AD-33): the two-bucket money model (income + deposits held) becomes THREE buckets, shown separately and never summed — EARNED (the only income), DEPOSITS HELD, RENT HELD (rent collected on bookings not yet finished). Q6 stands as written; RENT HELD gets its own dashboard question
- (decision) U-3 RESOLVED (AD-35): 'lost' is NO LONGER terminal for either channel. Exactly one arc leaves it, called recover, with three destinations (in_stock, in_maintenance, retired), guarded by INVENTORY.RECOVER_LOST held by ADMIN and MANAGER only. The unit keeps its original barcode and its frozen price snapshots; who/when/why are recorded with a mandatory reason. Real lifecycle change, not a clarification — unit-state-machine.md both diagrams and both tables
- (decision) U-3 consequence (AD-35): the money is NEVER reversed by a recovery. Booking stays WRITTEN_OFF, agreement keeps written_off_at, recognised rent and forfeited deposit stand. Only the stock loss reverses — stock value goes back up by the unit's snapshotted buying price. The mirror-image build (reverse the income, leave the stock written off) is the catastrophic wrong answer and is named as such
- (constraint) U-4 RESOLVED — ASSUMPTION CONFIRMED (Raviraj): a damage or cleaning charge collected at settlement IS income against that unit, and the repair or replacement it funds IS booked separately as a RENTAL_UPKEEP expense against the same unit. Two rows, never one netted figure, so both sides show on the unit's ledger. Moves out of SPEC.md Assumptions and becomes a constraint; AD-33's settlement close event is built on it
- (decision) U-5 RESOLVED (AD-32 + AD-34): a piece that never comes back is a WRITE-OFF, not just a forfeited deposit. The booking gets a fourth terminal state WRITTEN_OFF, the agreement gets written_off_at, and that one timestamp recognises RENT + THE FULL DEPOSIT as income on its shop day. Nothing goes back to the customer, so deposit_returned stays zero. No separate overdue row is ever emitted on a written-off booking — the overdue is what ate the deposit and counting both counts the same money twice
- (constraint) U-5 consequence: a unit reaching 'lost' is an AUDIT FACT and NEVER an income anchor, with an agreement or without one. written_off_at is the single anchor for both the rent and the forfeited deposit. Two anchors for one deposit is how the same money gets counted twice. The write-off and the unit's move to lost are one transaction — all three writes or none
- (decision) U-6 RESOLVED (AD-35): Q10 shrinkage NETS recoveries. Shrinkage is a plain sum of a signed shrinkage figure — negative the unit's snapshotted buying price on a transition into damaged/lost/retired, positive the same figure on a recovery out of lost, zero elsewhere — so a question cannot forget to net it. lost -> retired as a recovery destination nets to zero and still flags as a recovery
- (decision) U-7 RESOLVED: three new dashboard questions numbered after Q24, no retired id reused — Q25 RENT HELD (point-in-time, rent collected on bookings not yet finished), Q26 deposit-exhaustion review list (point-in-time, live agreements past due_date + floor(deposit/overduePerDay) days), Q27 recoveries (range, count and value of units recovered, valued at snapshotted buying price and never at the netted shrinkage figure). Catalogue goes 24 -> 27 questions
- (decision) U-7 detail (AD-34): the deposit-exhaustion date is a PROMPT FOR A HUMAN, never a trigger. It is derived on read as due_date + floor(deposit/overduePerDay) days and drives a review list only. Nothing in the system may branch on it beyond deciding whether a row appears in that list — no status change, no money movement, no row written when it passes. The no-scheduler constraint is untouched
- (decision) U-8 RESOLVED (Raviraj, via the spine's Deferred section, closed 2026-08-20): NO identity document is ever captured at rental hand-over. Rent plus deposit, both collected at booking, exceed the unit's buying price, so a piece that never comes back has already been paid for and there is nothing for an ID to secure. No id_type, id_reference or document-image column on rental agreements. The open question leaves SPEC.md permanently — recorded so it is not reopened
- (decision) U-9 RESOLVED (AD-8): the unique constraint on sale_lines.unit_id across non-reversing lines is DROPPED and is not added. It makes CAP-23 impossible — an exchange sells the same unit a second time on a new line, which the index would refuse. The race guard is the compare-and-swap on units.status inside the checkout transaction: the status change itself is the lock. Applies to SPEC.md's race-guard constraint as well as domain-model.md SaleLine, both carried the same claim
- (constraint) U-9 scope note: the rental-side guard SURVIVES — at most one open agreement per unit stays a database fact (AD-9), now with 'and not written off' in the predicate so a written-off agreement releases the unit's slot. Only the sale-line index is dropped
- (decision) U-10 RESOLVED (AD-23): rental days are (endDate - startDate) + 1 EVERYWHERE — rent charged at booking, the maximum-period check, and utilisation. A 15th-17th booking is three days, matching the inclusive date-range hold and the physical fact that the piece is out of the shop on all three days. domain-model.md's endDate - startDate understated it by one day, silently in the customer's favour
- (decision) U-11 RESOLVED (AD-18): Q21 new-vs-returning customers matches on the sale's customer id, NOT the WhatsApp number. The number is released on erasure, so matching on it detaches an erased customer's history and merges a new person who later reuses that number into the old one's sales. The customer id survives erasure intact
- (note) U-12 NOT ACTIONED HERE: it lands in ux-IMPOC-2026-08-20/EXPERIENCE.md ('CASHIER sees all of Counter'), which is an adopted companion owned by bmad-ux, not a spec-authored file. bmad-spec does not edit adopted companions. Raised to Raviraj as a separate bmad-ux run; AD-29 says navigation is permission-driven so no UI rework follows, only the sentence
- (decision) Recovery is NOT a new capability id. The spine's own capability map files AD-35 under CAP-10 and CAP-11 (units module), so the recover arc extends CAP-11's success criterion rather than claiming CAP-25. Capability set stays at 23 live, CAP-16 still retired
- (decision) ARCHITECTURE-SPINE.md re-adopted as a companion of SPEC.md at ../../planning-artifacts/architecture/architecture-IMPOC-2026-08-20/ARCHITECTURE-SPINE.md — adopted, not spec-authored, so bmad-spec references it and never edits it. Every AD id in it stays as numbered
- (decision) Glossary updated beyond the four named files, because leaving it alone would have left it contradicting the new rules: Shrinkage (now net of recoveries), Deposit (whole deposit becomes income on a write-off), Final ROI (includes overdue; anchored to the write-off) and Booking (rent held, not earned) were all stale, and six new terms added — Rental days, Finishing event, Rent held, Write-off, Deposit-exhaustion date, Recovery
- (note) brownfield.md swept and needed no change — none of U-1..U-12 touches the five barcode/scanner defects or the permission-fit notes
- (note) Two stale race-guard claims found beyond the U-9 line-item during the sweep and fixed: SPEC.md's in_cart constraint and unit-state-machine.md's opening in_cart paragraph both named the sale-line unique index as the guard. U-9 only cited domain-model.md
- (question) AD-31 (a booking may be EXTENDED while OPEN, never shortened, difference collected at the counter, rent never refunded) is a real user-facing behaviour the spec has no capability for, and it is NOT in the U-1..U-12 table — the spine did not flag it as a conflict. Raviraj to decide whether amending a booking becomes CAP-25 or stays an architecture-level allowance under CAP-17
- (event) self-validate pass 1 (coherence): PASS — 23 live capabilities (CAP-16 still retired, no id reused, no new id claimed), every one carrying intent and success; 26 constraints, each ruling something out; 14 non-goals; success signal extended to name the rent-held bucket so the three-bucket rule is demonstrable, not just asserted; dashboard catalogue runs Q1..Q27 with no gap and no reuse; all 6 spec-authored + 4 adopted companions resolve on disk, the spine included
- (event) self-validate pass 2 (preservation): PASS — U-1..U-11 all landed and were re-swept by grep for their stale phrasings; zero survivors of 'bookings made in range', 'Rent is revenue', 'lost is terminal', 'endDate - startDate' as a bare day count, the sale-line unique index, 'matched on whatsappNumber', or the identity-document open question. U-12 is out of scope for this skill (UX-owned adopted companion) and is the only item not actioned. Three fixes beyond the table: two extra copies of the U-9 race-guard claim, and the glossary
- (event) spec re-derived: SPEC.md + domain-model.md + dashboard-questions.md + unit-state-machine.md + glossary.md. Assumptions and Open Questions sections both removed — nothing outstanding for the first time. ARCHITECTURE-SPINE.md adopted as a companion with an architecture-precedence note. No stories.yaml exists, so nothing to re-check there. sources: [] — the original input was an in-chat brain dump
- (note) Left for bmad-architecture, not fixable here: the spine's own Upstream Conflicts table is now satisfied for U-1..U-11 and can be cleared, and its AD-12/AD-14/AD-26 still say 'all 24 dashboard questions' where the catalogue now runs to 27
- (event) CORRECTS the earlier 're-derived' entry: Assumptions is empty and its section is gone, but Open Questions is NOT — the AD-31 booking-amendment gap was written into SPEC.md as one open question rather than left in the memlog alone, since a downstream builder reads SPEC.md and would otherwise never see it. Final state: 0 assumptions, 1 open question
- (direction) Raviraj: fix the two upstream conflicts the architecture spine still flags at AD-8 and AD-18 (working number AD-14) — the checkout race-guard wording and Q21's customer match — closing the gap between what the spec's prose says and what the spine's Upstream Conflicts table still lists as open
- (decision) FIX 1 (AD-8): U-9's literal wrong phrase ('unique constraint on unitId across non-reversing sale lines') was already dropped from domain-model.md and SPEC.md in a prior run, but the corrected rule was never written down as an explicit invariant with allowed/blocked cases — a builder reading only the prose could still misjudge the resale-after-exchange path. Named precisely now: at most one STANDING sale line per unit at any time (a line not itself reversed by a later reversing line) — one word different from the old wrong wording ('standing' not 'non-reversing'), so a reversed line no longer counts and an exchanged unit is not barred from resale
- (constraint) Raviraj: NO cap on how many times one unit may be exchanged and resold — case 3 of the standing-line rule. Not expected to happen often, but the domain model must not encode a limit that isn't there
- (decision) Recorded in domain-model.md (SaleLine section): four allowed cases (first sale; resale after one exchange-return; unlimited repetitions of that cycle; a unit as the replacement piece in someone else's exchange) and five blocked cases (selling a currently-sold unit; selling damaged/lost/retired/in_maintenance; selling a rental-channel unit at retail; reversing one sale line twice; two checkouts completing on one unit at once). Noted that the unit's own status already carries the standing-line fact — sold means a standing line exists, in_stock means none does — so AD-8's compare-and-swap on units.status enforces the corrected rule rather than replacing it with nothing
- (decision) CAP-14 and CAP-23 success criteria (SPEC.md) both extended to name the resale-after-exchange case explicitly: CAP-14 states a previously-exchanged unit checks out like any other in_stock unit; CAP-23 states the returned unit is sellable/exchangeable again with no limit on repeat cycles. Closes the exact ambiguity the old wrong wording created, at the capability level and not only in domain-model.md
- (question) Flag to Raviraj, for bmad-architecture, not decided here: can the database enforce 'at most one standing sale line per unit' as a second layer behind the units.status compare-and-swap? It would likely need a column marking a sale line as reversed/cancelled (or a derived-from-reversesSaleLineId partial unique index shaped like AD-9's sale_lines_one_reversal), which brushes against the never-UPDATE-a-completed-transaction constraint since the line itself must stay untouched — the marker would have to live on the reversing row's linkage, not a mutation of the original. This is an architecture call, not a spec one; the spec's guard (unit status) is already correct and complete on its own
- (direction) Raviraj: FIX 2 (AD-18/U-11) — Q21 must match new-vs-returning customers on the internal id, not whatsappNumber, because erasure releases the number and a later person receiving that recycled number would be merged with the erased customer's purchase history. Indian mobile numbers are recycled, so this is a real outcome. Sweep every other companion for the same mistake; counter-time lookup by WhatsApp number is correct and must stay — only reporting/history joins move to the id
- (event) FIX 2 verification: Q21 already matches on Sale.customerId with an explicit 'never on whatsappNumber' clause (resolved as U-11/AD-18 in a prior update run). Swept dashboard-questions.md, domain-model.md, SPEC.md, glossary.md, unit-state-machine.md, brownfield.md for whatsappNumber/WhatsApp usage: every remaining hit is either the correct counter-time lookup key (CAP-13, domain-model.md Customer row), a transacted-record snapshot copied onto Sale/RentalAgreement at the moment of the transaction (not a reporting join), or the glossary's Snapshot definition. No reporting or history read anywhere matches a customer on phone number. No further edit needed for Fix 2 — the spec side of U-11 is already correct and complete; the spine's AD-18 marker is stale and clearable by bmad-architecture
- (event) self-validate pass 1 (coherence): PASS — 23 live capabilities unchanged (CAP-16 still retired, no id reused, no new id claimed); CAP-14 and CAP-23 both keep intent + success, success criteria extended not weakened; 26 constraints, each still ruling something out; 14 non-goals; success signal untouched; dashboard catalogue unchanged at Q1-Q27; all 6 spec-authored + 4 adopted companions resolve on disk
- (event) self-validate pass 2 (preservation): PASS — every element of Raviraj's instruction landed: the standing-line invariant and its four allowed / five blocked cases in domain-model.md SaleLine section; CAP-14 and CAP-23 success criteria both name the resale-after-exchange case; the architecture flag on second-layer DB enforcement recorded as a memlog question for Raviraj to route, not invented as an answer. Fix 2's sweep confirmed no further edit was load-bearing — Q21 and every other whatsappNumber reference were already correct from a prior update run. No wrapper-only content dropped
- (event) targeted re-derive complete: domain-model.md (SaleLine standing-line rule + case lists) + SPEC.md (CAP-14, CAP-23 success criteria). dashboard-questions.md, glossary.md, unit-state-machine.md, brownfield.md unchanged — swept, already correct. No stories.yaml exists. sources: [] so nothing upstream to sync. The two architecture spine markers this run targets (AD-8, AD-18 — working number AD-14) are unedited, per Raviraj's instruction that the spine is architecture-owned; both are now clearable by bmad-architecture
- (event) update run: input is five decisions from Raviraj (CAP-25 booking extension, till scan/bargain rhythm for CAP-12+CAP-14, settlement slip at rental return, adopt UX pair as companions, verify U-1..U-11) plus a re-check of the Upstream Conflicts table
- (capability) CAP-25 NEW (Raviraj): Extend a booking. Staff widen an open booking's window at either end, never narrow; the extended window must contain the original. Extra rent (recomputed over the new window, same rentalDays formula) is collected at the counter in the same gesture; no rent is ever refunded on an amendment. Re-checked against floor(deposit/overduePerDay) exactly as the original booking. A booking not still open cannot be extended. Raviraj's call: this is a real counter action that takes money, so it is its own capability with its own success criteria (architecture AD-31), not a build-level allowance inside CAP-17 — same reasoning that kept the recovery arc as an extension of CAP-11 rather than a new id, applied in the opposite direction because this one moves money and that one did not
- (decision) Open Question (AD-31 amendment) RETIRED now that CAP-25 exists to answer it. SPEC.md's Open Questions section is now empty and is removed — 0 assumptions, 0 open questions
- (decision) Non-goals extended: the never-refund-rent non-goal now names amendment alongside early return and cancellation, so all three rent-never-refunded paths (early return, cancellation, narrowed amendment attempt) are stated together rather than the amendment case being inferable only from CAP-25's success criteria
- (decision) Raviraj: the till's scan rhythm is NOT the intake rhythm. CAP-12 (scan-to-cart) is rewritten — the camera stays armed continuously, each decoded unit drops into the cart with no per-piece confirm tap, and pricing is not decided per scan. Floor-price enforcement moves entirely to a single confirm step at the end of the cart, before payment, which is CAP-14's job, not CAP-12's
- (decision) Raviraj: bargaining entry at the CAP-14 confirm screen is BOTH ways, cashier's choice per bill — one negotiated total for the whole cart that the app spreads across lines, or a price typed against each line individually. Either way every line must clear its own floor price; a bill that would push any single line below its floor is refused, naming that piece, before payment
- (question) Flagged to bmad-architecture, not decided here: the spreading rule for a whole-bill negotiated total across cart lines (proportional to each line's selling price is the obvious default) needs deciding. This is an architecture call — how the spread is computed and whether any intermediate figure is persisted — not a spec one. CAP-14's success criterion states the constraint the spread must satisfy (every line clears its own floor) without prescribing the algorithm
- (note) domain-model.md SaleLine needs one clarifying line: transactedPricePaise is the per-line result regardless of whether the confirm-step price was entered directly against the line or derived from spreading a whole-bill total — no separate whole-bill-total column is stored. Prevents a builder inventing a bill-level price field
- (note) EXPERIENCE.md's Scan Primitive section states (as an [ASSUMPTION]) that lot intake's one-scan-one-confirm cadence 'applies unchanged' to the retail cart, and its own Open Questions asks whether that cadence applies to the retail cart unchanged. Raviraj's till-scanning decision answers that question NO for the retail cart specifically — continuous scan, no per-piece confirm, single end-of-cart confirm. EXPERIENCE.md is an adopted companion owned by bmad-ux and is not edited by this run; this is raised as a new upstream conflict for bmad-ux to resolve, the same handling as U-12
- (decision) Raviraj: a settlement slip is produced at EVERY rental return, always — not only when money was deducted — showing deposit held, overdue deducted, damage deducted, and cash returned. Decision: this EXTENDS CAP-19's success criteria rather than becoming a new capability id. Reasoning: CAP-19 already is the money-moving action (it settles the deposit); the slip documents what CAP-19 just did, the same relationship CAP-18's own success criteria already has to its hand-over receipt (one receipt per group, bundled into CAP-18, not a separate capability) — unlike CAP-25, this is not itself a new counter action that takes money
- (decision) The settlement slip uses the same renderer and A5 layout as the sale receipt, per ux-IMPOC-2026-08-20/EXPERIENCE.md's Receipt Contract. That section already carried this exact document as an [ASSUMPTION] ('a settlement summary at rental return... uses the same layout and the same four channels... drop it if unwanted'). Raviraj's decision here CONFIRMS it rather than dropping it. EXPERIENCE.md is adopted and not edited by this run; flagged to bmad-ux to convert its [ASSUMPTION] to a firm decision, mirroring how U-4 moved SPEC.md's own damage-charge assumption to a confirmed constraint
- (decision) Raviraj: adopt DESIGN.md and EXPERIENCE.md (ux-IMPOC-2026-08-20/) as companions of SPEC.md by relative path, owned by bmad-ux, referenced never edited — without this, downstream story writing cannot see the design at all. companions: frontmatter now lists both alongside the existing spec-authored and adopted companions
- (event) U-1..U-11 re-swept against current SPEC.md + companions (Raviraj's instruction, item 5). All 11 remain genuinely satisfied: U-1/U-2 (CAP-17, CAP-22, domain-model money rule 3, dashboard-questions Q1/Q6/Q25, glossary all reflect close-event recognition and three buckets); U-3 (unit-state-machine.md both channels show the recover arc, lost non-terminal, CAP-11); U-4 (damage-charge treatment is a Constraint in SPEC.md, not an Assumption — Assumptions section no longer exists); U-5 (CAP-19, unit-state-machine.md rented->lost row, dashboard-questions Q1/Q23 all anchor rent+deposit forfeiture to writtenOffAt alone); U-6 (dashboard-questions Q10 nets recoveries via signed shrinkage_paise, Q27 shows recoveries separately); U-7 (Q25 rent held, Q26 deposit-exhaustion review list, Q27 recoveries all present, catalogue runs Q1-Q27 with no gap); U-8 (SPEC.md Open Questions carries no identity-document entry); U-9 (domain-model.md SaleLine states the standing-line rule precisely, no unique index on unit_id; CAP-14/CAP-23 both name the resale-after-exchange case); U-10 (rental days (endDate-startDate)+1 stated everywhere it is used — CAP-17, domain-model.md, glossary); U-11 (dashboard-questions Q21 matches on Sale.customerId, explicit never-on-whatsappNumber clause). Nothing stale found. The spine's own Upstream Conflicts table (architecture-owned, not edited here) still shows these as open on its face — that table is now fully clearable by bmad-architecture
- (event) self-validate pass 1 (coherence): PASS — 24 live capabilities (CAP-1..CAP-15, CAP-17..CAP-25; CAP-16 still retired, no id reused), every one carrying intent and success; CAP-25 is new and its success criterion is falsifiable (contains-the-original, extend-only, re-checked against max period); CAP-12 and CAP-14 rewritten together so the scan-vs-price responsibility split is unambiguous across the two; CAP-19 extended with the settlement-slip requirement without weakening its existing criteria; 27 constraints (one non-goal line extended, ruling out amendment-refund specifically); 14 non-goals; success signal extended to demonstrate the till rhythm, bargaining, extension, and settlement slip end-to-end; Open Questions and Assumptions sections both empty and both removed; dashboard catalogue unchanged at Q1-Q27 (CAP-25 needs no new question — Q25 already reads live rentChargedPaise); all 6 spec-authored + 6 adopted companions (DESIGN.md and EXPERIENCE.md newly adopted) resolve on disk
- (event) self-validate pass 2 (preservation): PASS — all five of Raviraj's decisions landed: CAP-25 in SPEC.md + domain-model.md (extension rule note) + glossary.md (Extension term) + Non-goals; CAP-12/CAP-14 rewritten in SPEC.md + domain-model.md (SaleLine spread-vs-per-line note); CAP-19 + settlement slip in SPEC.md and success signal; DESIGN.md/EXPERIENCE.md added to companions: frontmatter, unedited themselves; U-1..U-11 re-verified against current SPEC.md and every companion, all still satisfied, nothing stale, logged as its own event. Two items intentionally NOT actioned in spec-owned files, both flagged instead: the whole-bill spreading formula (architecture decision, memlog question for bmad-architecture) and EXPERIENCE.md's own scan-cadence assumption plus its settlement-summary [ASSUMPTION] (UX-owned adopted companion, not edited here, flagged to bmad-ux as new upstream conflicts alongside the still-open U-12). No wrapper-only content dropped
- (event) targeted re-derive complete: SPEC.md (frontmatter companions, CAP-12, CAP-14, CAP-19, CAP-25, one Non-goals line, Success signal, Open Questions section removed) + domain-model.md (RentalBooking extension rule, SaleLine spread note) + glossary.md (Extension term). unit-state-machine.md, dashboard-questions.md, brownfield.md swept and needed no change. No stories.yaml exists. sources: [] so nothing upstream to sync. Companions now 6 spec-authored + 6 adopted (10 total, plus the 3 backend/frontend/root AGENTS.md already counted among adopted)
- (event) update run: input is Raviraj's decision to replace the payment-method dropdown with self-generated UPI QR codes for CAP-14/CAP-15, a new payment-reference concept, two new money-safety constraints, a general-vs-retail-only scope question for the QR mechanism, and a new ESP32 wireless-payment-display non-goal
- (decision) Raviraj: UPI confirmation is by human visual check, not a gateway callback. Cashier generates the UPI QR himself; customer scans with her own UPI app and pays; she shows her phone's success screen; cashier checks it with his own eyes and taps Mark as received — only then does the sale commit. No payment provider is ever contacted and the app never claims to know whether a payment succeeded; the existing no-payment-gateway constraint stays fully intact
- (decision) QR content: a standard UPI deep link carrying the payee address, the shop name as payee name, the amount, INR as currency, and a transaction note. The note carries the payment reference so Raviraj can match takings against his bank statement
- (decision) NEW CONCEPT — Payment reference (not an invoice number). Short, human-readable, sequential; allocated from a Postgres sequence at the moment the QR is drawn; stored on the sale/booking only when it commits. Because nothing is written before Mark as received, an abandoned or cash-switched checkout burns a reference and leaves a gap. Gaps are expected and harmless — reconciliation matches on references that appear, never on a complete run
- (constraint) Payment reference is deliberately NOT the invoice number. GST is a current non-goal but the spec already carries a do-not-block-later note, and Indian GST requires consecutively numbered invoices with no gaps. A gapped payment-reference series would be a compliance problem the day the shop registers, so if an invoice number is ever introduced it is a separate identifier allocated at commit, from its own sequence, never merged with the payment reference
- (decision) CAP-14 flow rewritten. After the single pricing/floor-check confirm step, the cashier picks the payment method. On UPI the QR renders on the checkout screen and nothing is written to the database while it is displayed. Three outcomes: (a) Mark as received — the sale commits in one transaction exactly as CAP-14 already specified (in-stock re-verification, compare-and-swap, snapshots), now also storing paymentMethod=UPI and the payment reference; (b) UPI is down on either side — cashier switches method to cash, dismisses the QR, marks received — one sale recorded as cash, the already-allocated reference is abandoned as a gap; (c) customer decides not to buy — cashier cancels checkout, cart clears, and because nothing was ever written there is no record to delete and no half-finished sale to clean up. That last property is the reason for this design
- (constraint) The amount encoded in a UPI link is a request, not a lock — some UPI apps let the payer edit it before paying. This is precisely why the cashier's visual check of the customer's phone success screen is the actual control; the QR itself is never treated as proof of payment
- (constraint) UPI links carry rupees with two decimals; the system stores integer paise. There must be a single, exact conversion point between the two — a mismatch between the QR's displayed amount and the recorded sale is the worst-case bug available in this feature
- (decision) CAP-15 (sale receipt) extended: the receipt now also carries the payment reference, since that is what ties a receipt back to a bank statement line
- (decision) Raviraj resolved (not assumed): the UPI QR mechanism is a GENERAL payment surface, not retail-only. Reasoning: RentalBooking already records paymentMethod UPI/CASH exactly like Sale, and CAP-17 (booking), CAP-18 (walk-in hand-over) and CAP-25 (extension) all collect money at the counter the same way retail checkout does. The same draw-QR / nothing-written-until-confirmed / mark-as-received flow and payment-reference allocation applies at CAP-14, CAP-17, CAP-18's walk-in path, and CAP-25, wherever UPI is chosen. Recorded once as a Constraint rather than rewritten into each capability's prose, to avoid stating the same mechanism four times
- (decision) Non-goals extended, same shape as the loyalty and GST entries: a wireless payment-status display (an ESP32 device with its own screen showing the current QR and a payment-received animation, driven from the POS) is out of scope now — the QR renders on the checkout screen itself. Do-not-design-against-it note: keep the UPI link's content reproducible server-side, from the payment reference alone, so such a device could fetch it later. Nothing is built for it now
- (question) Flagged to bmad-architecture, not decided here: the payment-reference Postgres sequence (naming, format, human-readable shape), the qr-code-styling library Raviraj has already chosen for rendering, where the UPI link string is assembled and URL-encoded, and how the reference reaches the client before any sale row exists (it must be visible on the rendered QR before a sale or booking row is committed). These are build-level calls; CAP-14's success criterion and the new constraints state what the sequence and the conversion point must guarantee, not how
- (note) domain-model.md Sale table gets a new paymentReferenceCode field (set only when paymentMethod=UPI) plus a callout distinguishing it from a future invoice number. RentalBooking table gets the same field, consistent with the general-payment-surface decision. Money and derivation rules section gets a new rule naming the single paise-to-rupee conversion point for the UPI link. Glossary gets a new Payment reference term
- (event) self-validate pass 1 (coherence): PASS — 25 live capabilities unchanged (CAP-1..CAP-15, CAP-17..CAP-25; CAP-16 still retired, no id reused, no new id claimed — CAP-14/CAP-15 rewritten, not renumbered); CAP-14 keeps intent + success, success now describes the QR/mark-as-received flow and its three outcomes concretely (falsifiable: DB write only after commit, reference only stored on commit); CAP-15 extended with the payment reference, not weakened; 31 constraints, each still ruling something out (visual-confirmation-not-gateway, QR-amount-not-a-lock, single exact paise/rupee conversion point, payment-reference-not-invoice-number, general-payment-surface scope); 15 non-goals (ESP32 display new, same shape as loyalty/GST); Success signal extended with the QR beat; dashboard-questions.md Q2 re-checked and still correct unedited (paymentMethod, not paymentReferenceCode, is its subject); all 6 spec-authored + 4 adopted companions resolve on disk
- (event) self-validate pass 2 (preservation): PASS — every element of Raviraj's instruction landed: human-eye confirmation stated in those terms (CAP-14 success + Constraints); QR content (payee address, shop name, amount, INR, note-with-reference) in CAP-14; payment reference concept, its Postgres-sequence allocation, its gap tolerance, and its explicit non-invoice-number distinction with the GST reasoning, in both SPEC.md Constraints and domain-model.md's Sale table callout; the three-outcome flow (mark received / switch to cash / cancel) with the nothing-written-until-commit property named as the reason, in CAP-14; the two new constraints (amount-is-a-request-not-a-lock, single exact paise-rupee conversion point) in Constraints and domain-model.md money rule 7; CAP-15 receipt now names the payment reference; the general-vs-retail-only question resolved as general and the serving capabilities (CAP-14, CAP-17, CAP-18, CAP-25) named in one Constraints line rather than duplicated four times; the ESP32 non-goal added in the loyalty/GST shape with its reproducible-server-side implication; the four architecture flags (sequence design, qr-code-styling library, link-assembly location, reference-before-sale-row delivery) logged as a memlog question, not decided in SPEC.md. RentalBooking.paymentReferenceCode and glossary's Payment reference term added for consistency with the general-surface decision, beyond the literal ask but load-bearing for it. No wrapper-only content dropped
- (event) targeted re-derive complete: SPEC.md (CAP-14, CAP-15, five new/extended Constraints lines, one new Non-goals line, Success signal) + domain-model.md (Sale.paymentReferenceCode + invoice-number callout, RentalBooking.paymentReferenceCode, money rule 7 on the conversion point) + glossary.md (Payment reference term). unit-state-machine.md, dashboard-questions.md, brownfield.md swept and needed no change — the checkout state transition and Q2's till-reconciliation question are both paymentMethod-level, unaffected by the reference concept. No stories.yaml exists. sources: [] so nothing upstream to sync. Four items intentionally NOT decided in spec-owned files, flagged instead as a memlog question for bmad-architecture: the payment-reference sequence's naming/format, the qr-code-styling library, where the UPI link is assembled/encoded, and how the reference reaches the client before any sale/booking row exists
